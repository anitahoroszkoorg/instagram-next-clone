import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';

// TODO


// describe('LoginPage Component', () => {
//   it('should render the login title, form, and sign-up link', () => {
//     render(<LoginPage />); // Render the LoginPage component

//     expect(screen.getByText(/log in to instagram/i)).toBeInTheDocument();

//     // Check if the form is rendered
//     expect(screen.getByTestId('login-form')).toBeInTheDocument();

//     // Check if the sign-up link is displayed with the correct URL
//     const signUpLink = screen.getByRole('link', { name: /don't have an account\? sign up/i });
//     expect(signUpLink).toBeInTheDocument();
//     expect(signUpLink).toHaveAttribute('href', '/register');
//   });
// });
